//function to validate registration form
function validate registration () {
    const username=document.get element by id('user name')values:
    const password=document.get element by id('password')values:
    if(use name.trim()==="//password trim ()==="){
        alert("please enter both username and password");
        return false;
    }
    //Additional validation logic can be added as needed
    return true;
}
